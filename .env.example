# Copy this file to `.env` and update the values.
# IMPORTANT: do not commit your real `.env` (it's ignored by .gitignore).

# Use psycopg3 driver
DATABASE_URL=postgresql+psycopg://postgres:your_password@localhost:5432/simple_login_db

